{
  "framework": "bun",
  "buildCommand": "bun build src/web/index.ts --outdir=dist --target=bun",
  "outputDirectory": "dist",
  "installCommand": "bun install",
  "routes": [
    { "src": "/(.*)", "dest": "/dist/$1" }
  ],
  "env": {
    "CLAUDE_API_KEY": "@claude-api-key"
  }
}